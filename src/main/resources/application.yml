spring:
    application:
        name: room-rental-service
    config:
        import: "optional:configserver:"
    cloud:
        config:
            uri: http://localhost:8081/
    profiles:
        active: local